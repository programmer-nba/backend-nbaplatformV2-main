version: 2
jobs:
   deploy:
      docker:
         - image: circleci/node:10.16.3
      steps:
         - run: ssh -oStrictHostKeyChecking=no -v  $SERVER_USER@$SERVER_IP "./deploy-backend-nbaplatformV2-main.sh"
workflows:
version: 2
   build&&deploy:
      jobs:
         - deploy:
      filters:
         branches:
            only: mian

